---
- name: WildFly with Docker
  hosts: all
  vars:
    deployables:
      jolokia:
        group-id: org.jolokia
        artifact-id: jolokia-war
      ping:

  roles:
  - role: t1.jee_worker # TODO publish this role into Galaxy
    # magic trick:
    # cd ~/.ansible/roles
    # ln -s ~/workspace/t1/deployer/src/main/ansible/jee-worker t1.jee_worker

  tasks:
